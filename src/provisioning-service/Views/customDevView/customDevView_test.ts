'use strict';

describe('provisionerPortal.customDevView module', function() {

  beforeEach(module('provisionerPortal.customDevView'));

  describe('customDevView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var customDevViewCtrl = $controller('customDevViewCtrl');
      expect(customDevViewCtrl).toBeDefined();
    }));

  });
});