{
  "name": "provisioning-service",
  "version": "0.0.0",
  "description": "installing azure resources",
  "main": " ",
  "scripts": {
    "test": "karma start karma.conf.js",
    "start": "npm run build && http-server -a localhost -p 8000 -c-1 ./src/provisioning-service",
    "build": "tsc"
  },
  "author": "chrande@microsoft.com",
  "license": "MIT",
  "dependencies": {
    "azure-arm-resource": "^1.4.4-preview",
    "bluebird": "^3.4.0",
    "ms-rest-azure": "^1.14.4",
  }
}
