'use strict';

describe('provisionerPortal.configurationsView module', function() {

  beforeEach(module('provisionerPortal.configurationsView'));

  describe('configurationsView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var configurationsViewCtrl = $controller('configurationsViewCtrl');
      expect(configurationsViewCtrl).toBeDefined();
    }));

  });
});